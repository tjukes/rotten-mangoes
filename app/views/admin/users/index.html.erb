<h1>Rotten Mangoes</h1>
<hr>

<h2>Users</h2>

<% @users.each do |user| %>
  <div>
    <%= "#{user.firstname} #{user.lastname}" %>
    <%= user.email %>
    <% if user.is_admin? %>
      Admin
    <% end %>
  </div>
<% end %>
